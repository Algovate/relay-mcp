# Swagger/OpenAPI Spec Source
# Options: 'file' or 'url'
SWAGGER_SOURCE=url

# If using URL source
SWAGGER_URL=https://petstore.swagger.io/v2/swagger.json

# If using file source
# SWAGGER_FILE=./specs/api.json

# API Base URL (optional - will use URL from spec if not provided)
# API_BASE_URL=https://api.example.com

# Authentication Type
# Options: 'none', 'apikey', 'bearer', 'basic'
AUTH_TYPE=none

# API Key Authentication (if AUTH_TYPE=apikey)
# AUTH_API_KEY=your-api-key-here
# AUTH_API_KEY_NAME=X-API-Key
# AUTH_API_KEY_IN=header

# Bearer Token Authentication (if AUTH_TYPE=bearer)
# AUTH_TOKEN=your-bearer-token-here

# Basic Authentication (if AUTH_TYPE=basic)
# AUTH_USERNAME=your-username
# AUTH_PASSWORD=your-password

# Request timeout in milliseconds
TIMEOUT=30000
